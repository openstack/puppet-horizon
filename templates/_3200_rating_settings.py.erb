from django.conf import settings


# Sample
# settings.LOGGING['loggers'].update({
#     'cloudkittyclient': {
#         'handlers': ['console'],
#         'level': 'DEBUG',
#         'propagate': False,
#     }
# })
settings.LOGGING['loggers'].update({
    'cloudkittyclient': {
        # 'handlers': ['console'],
        'handlers': ['<%= @log_handlers.join("', '") %>'],
        # level': 'DEBUG',
        'level': '<%= @log_level %>',
        'propagate': False,
    }
})

# OPENSTACK_CLOUDKITTY_RATE_PREFIX = <None>
<%- if ! @rate_prefix.nil? -%>
OPENSTACK_CLOUDKITTY_RATE_PREFIX = '<%= @rate_prefix %>'
<%- end -%>

# OPENSTACK_CLOUDKITTY_RATE_POSTFIX = <None>
<%- if ! @rate_postfix.nil? -%>
OPENSTACK_CLOUDKITTY_RATE_POSTFIX = '<%= @rate_postfix %>'
<%- end -%>

# CLOUDKITTY_QUOTATION_SERVICE = 'instance'
<%- if ! @quotation_service.nil? -%>
CLOUDKITTY_QUOTATION_SERVICE = '<%= @quotation_service %>'
<%- end -%>
